
{
  "name": "harris-county-youth-humanitarian-award",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "react-router-dom": "^7.11.0",
    "@google/genai": "^1.34.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "scripts": {
    "build": "mkdir -p build && esbuild index.tsx --bundle --outfile=build/index.js --format=esm --loader:.tsx=tsx --loader:.ts=ts --minify --platform=browser --external:react --external:react-dom --external:react-router-dom --external:@google/genai && cp index.html build/ && (test -d public && cp -r public/* build/ || true)"
  }
}
